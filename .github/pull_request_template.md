## What changed

Describe the smallest useful unit of change.

## Why it matters

Name the fidelity gate, numerical risk, or contributor need this addresses.

## Verification

- [ ] `uv run ruff check .`
- [ ] `uv run pytest -q`
- [ ] New or changed numerical claims include the command, grid, timestep, and resolution checks.
- [ ] Large checkpoints remain outside Git; compact result media and data are updated if needed.
- [ ] Documentation still states that this is a finite surrogate, not an independent proof.
